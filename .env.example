# Drizzle
DATABASE_URL=data/geeft.sqlite

ORIGIN="http://localhost:5173"

# Better Auth — 32+ random characters in production
BETTER_AUTH_SECRET=""

# SMTP2GO — transactional mail (reset / password changed). Leave empty in local dev.
SMTP2GO_API_KEY=""
MAIL_FROM="Geeft <noreply@geeft.app>"
